/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath:"",
    output:'export',
    images:{
        unoptimized:true
    },
    // assetPrefix:'/deerhack24-coming-soon',
};

export default nextConfig;
